name: educational_games_rating_table
description: Educational Games Rating Table.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^1.3.0
  cloud_firestore: ^2.2.2
  dotted_border: ^2.0.0
  collection: ^1.15.0
  sliver_tools: ^0.2.5
  adaptive_breakpoints: ^0.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - images/appbar_background.png
